# LAB07 Notes — Integrate the Mini-Lakehouse

## What I did

I connected the accumulated project components into the dependency-ordered pipeline and exercised the release/recovery path. The integration uses the preserved outputs from earlier days rather than rebuilding an unrelated fresh dataset.

## What I observed

The saved recovery/integration checks all passed:
- `injected_failure_observed = true`
- `previous_release_preserved = true`
- `rebuild_has_new_identity = true`
- `content_equal = true`

The deliberately injected failure was observed before unsafe downstream publication. The previously approved release remained available. A subsequent recovery/rebuild produced a new release identity while reconciling to the same logical business content rather than comparing volatile run timestamps.

## Recovery attempt

The recorded recovery attempt was:
1. Preserve the previously approved release.
2. Trigger the controlled failure and confirm downstream publication does not replace the approved release.
3. Correct/retry the dependency-ordered build.
4. Publish a new release identity only after the checks pass.
5. Compare logical business contents and confirm they reconcile.

This demonstrates recovery without erasing the evidence of the failed run.

## Decisions

The project is executed in dependency order from Bronze to trusted Silver and then to Gold/serving outputs. Publication is treated as a controlled release step: failure must leave the last approved release intact.

For rerun comparison, I use stable business content rather than run IDs or timestamps because operational metadata is expected to change between executions.

## Blockers / issues

The injected failure was intentional. It was successfully contained, and the recovery run passed the saved checks.

## Evidence

Primary evidence is retained in the executed Day 5 notebook outputs and the release/recovery evidence generated by the integration lab.