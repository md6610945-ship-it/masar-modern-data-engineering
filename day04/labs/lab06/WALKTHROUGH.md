<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Lab 06 · Make the quality decision explicit</h1><p><a href="../../README.md">Day 4</a> · <a href="../../STUDENT.ipynb">Student notebook</a> · <a href="../../QUALITY_POLICY.md">Quality rules</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اللاب 06 · اجعل قرار الجودة واضحًا</h1><p><a href="../../README.md">اليوم الرابع</a> · <a href="../../STUDENT.ipynb">دفتر الطالب</a> · <a href="../../QUALITY_POLICY.md">قواعد الجودة</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>1. Run the 06 cell after streaming</h2><p>The third code cell calls <code>run_quality_lab(spark, SOURCE, WORK)</code>. It verifies the real Day 3 Silver table and report before evaluating quality. The starting population is 75 unique trips with SAR 1,880.60 in fares.</p><p>Read <a href="../../../src/masar/quality_gate.py">quality_gate.py</a> and <a href="../../QUALITY_POLICY.md">the policy</a> alongside this guide. The notebook already calls the shared implementation; do not duplicate it. The four code cells are setup, streaming, quality and handoff.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>١. شغّل خلية 06 بعد التدفق</h2><p>تستدعي الخلية البرمجية الثالثة <code>run_quality_lab(spark, SOURCE, WORK)</code>. تتحقق من جدول Silver الفعلي وتقرير اليوم الثالث قبل فحص الجودة. تبدأ بـ75 رحلة فريدة وإجمالي أجور 1,880.60 ريالًا.</p><p>اقرأ <a href="../../../src/masar/quality_gate.py">quality_gate.py</a> و<a href="../../QUALITY_POLICY.md">السياسة</a> مع هذا الدليل. يستدعي الدفتر التنفيذ المشترك بالفعل؛ لا تكرره. الخلايا البرمجية الأربع هي الإعداد والتدفق والجودة والحفظ.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>2. Understand what GX validates</h2><p>The helper reads Delta and collects a bounded snapshot, no more than 500 trip rows, into Pandas. GX checks the 12 business columns, expected count, missing values, unique trip keys, known drivers, permitted categories and numeric/time relationships.</p><p>Additional row-policy checks parse timestamps, explain root defects and determine whether approval is allowed. This combines actual GX checks with explicit application rules; it is not distributed GX validation on an unlimited Spark table.</p><p>A GX Checkpoint runs validation and actions such as Data Docs generation. It is unrelated to Spark's streaming checkpoint directory. [<a href="../../SOURCES.md">S6–S9</a>]</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٢. افهم ما يفحصه GX</h2><p>تقرأ الدالة Delta وتجمع لقطة محدودة لا تزيد على 500 صف رحلة في Pandas. يفحص GX أعمدة الأعمال الـ12 والعدد المتوقع والقيم المفقودة وتفرد الرحلات والسائقين المعروفين والفئات والقيم والعلاقات الزمنية.</p><p>تفسّر فحوص سياسة الصفوف الإضافية التوقيتات وأسباب العيوب وتحدد السماح بالاعتماد. يجمع اللاب فحوص GX الفعلية وقواعد تطبيق صريحة؛ وليس فحص GX موزعًا لجدول Spark غير محدود.</p><p>يشغّل GX Checkpoint الفحص وإجراءات مثل توليد Data Docs، ولا يعني مجلد نقطة تحقق التدفق في Spark. [<a href="../../SOURCES.md">S6–S9</a>]</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>3. Trusted: validate the existing 75 trips</h2><p>The <code>trusted</code> phase must pass both GX and the row policy. It saves its own Checkpoint result and local HTML Data Docs. No source rows are modified.</p><p>All 75 rows being valid is not enough if the required population were missing rows. The exact 75-row requirement belongs to this fixed dataset, not to every real pipeline.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٣. trusted: افحص الرحلات الـ75 القائمة</h2><p>يجب نجاح GX وسياسة الصفوف في مرحلة <code>trusted</code>. تحفظ المرحلة نتيجة Checkpoint وData Docs محلية خاصة بها، دون تعديل صفوف المصدر.</p><p>صلاحية كل صف موجود لا تكفي عند فقدان جزء من الدفعة المطلوبة. العدد 75 شرط لهذه البيانات الثابتة، لا لكل خط بيانات حقيقي.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>4. Mixed: observe the intended failure</h2><p>The helper adds the seven supplied test rows to a <strong>new staging candidate</strong>, not to trusted Silver. This creates 82 rows. GX and the policy must reject that candidate.</p><p>Read the seven root reasons: <code>MISSING_TRIP_ID</code>, <code>INVALID_FARE</code>, <code>UNKNOWN_DRIVER</code>, <code>INVALID_TIMESTAMP</code>, <code>INVALID_DURATION</code>, <code>INVALID_DISTANCE</code> and <code>INVALID_CITY</code>. One root defect can trigger several GX assertions; do not expect exactly seven failed GX expectations.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٤. mixed: لاحظ الفشل المقصود</h2><p>تضيف الدالة صفوف الاختبار السبعة إلى <strong>نسخة مرشحة جديدة في التهيئة</strong>، لا إلى Silver الأساسية. يصبح العدد 82 صفًا. يجب أن يرفض GX والسياسة هذه النسخة.</p><p>اقرأ أسباب العيوب السبعة: <code>MISSING_TRIP_ID</code> و<code>INVALID_FARE</code> و<code>UNKNOWN_DRIVER</code> و<code>INVALID_TIMESTAMP</code> و<code>INVALID_DURATION</code> و<code>INVALID_DISTANCE</code> و<code>INVALID_CITY</code>. قد يفشل العيب الجذري الواحد في عدة قواعد GX؛ فلا تتوقع سبع قواعد GX فاشلة بالضبط.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>5. Quarantine with reasons</h2><p>The application writes seven rejected records to a separate Delta quarantine table and reads it back. The printed table includes <code>candidate_row</code>, <code>trip_id</code>, <code>reason_codes</code>, <code>raw_business_json</code>, <code>rule_version</code> and <code>candidate_path</code>.</p><p><code>raw_business_json</code> preserves the projected candidate row for diagnosis; it is not the original CSV text. Original source files remain separate and unchanged. A missing trip ID can appear as null. Do not fill it with an invented ID or discard the failure evidence.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٥. اعزل مع بيان السبب</h2><p>يكتب التطبيق السجلات المرفوضة السبعة في جدول Delta معزول ويعيد قراءته. يتضمن الجدول المطبوع <code>candidate_row</code> و<code>trip_id</code> و<code>reason_codes</code> و<code>raw_business_json</code> و<code>rule_version</code> و<code>candidate_path</code>.</p><p>يحفظ <code>raw_business_json</code> صف الأعمال المرشح بعد تهيئته للتشخيص؛ وليس النص الأصلي لملف CSV. تبقى المصادر الأصلية منفصلة ودون تعديل. قد يظهر معرف الرحلة المفقود بقيمة null؛ لا تستبدله بمعرف مختلق أو تتخلص من دليل الفشل.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>6. Rechecked: validate again before approval</h2><p>The 75 accepted rows become a <strong>new candidate</strong> and receive their own <code>rechecked</code> GX result and row-policy evaluation. Only after both pass and the full business contents match trusted Silver does the helper write a new approved Delta snapshot.</p><p>Check <code>Approved rows: 75</code> and all nine checks. The approved fare total remains SAR 1,880.60. The failed candidate is not relabeled as successful, and the original Silver table is not overwritten.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٦. rechecked: أعد الفحص قبل الاعتماد</h2><p>تصبح الصفوف المقبولة الـ75 <strong>نسخة مرشحة جديدة</strong> لها نتيجة GX باسم <code>rechecked</code> وتقييم مستقل لسياسة الصفوف. لا تكتب الدالة لقطة Delta معتمدة جديدة إلا بعد نجاح الفحصين ومطابقة محتوى الأعمال كاملًا مع Silver الأساسية.</p><p>تحقق من <code>Approved rows: 75</code> ونجاح الفحوص التسعة. يبقى إجمالي الأجور 1,880.60 ريالًا. لا تُحوّل نتيجة الدفعة الفاشلة إلى نجاح، ولا يُستبدل جدول Silver الأصلي.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>7. Read the reports and Data Docs</h2><p>Open <code>WORK/reports/day04_quality_latest.json</code>. The fields <code>candidate_table</code>, <code>quarantine_table</code>, <code>approved_table</code> and <code>reports</code> locate this run's data and reports relative to <code>WORK</code>.</p><p>Under the reported directory, each of <code>gx_trusted/</code>, <code>gx_mixed/</code> and <code>gx_rechecked/</code> contains <code>checkpoint_result.json</code> and its GX project. Use the <code>data_docs</code> paths in <code>gx_results</code> relative to that phase's GX folder to open the generated <code>index.html</code>. Keep the whole folder so its linked assets and validation pages remain available.</p><p>Interpret <code>distribution</code> and <code>scenario_freshness</code> using <a href="../../OBSERVABILITY.md">monitoring</a>. Scenario dates are fixed historical teaching inputs, not measured live-delivery latency.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٧. اقرأ التقارير وData Docs</h2><p>افتح <code>WORK/reports/day04_quality_latest.json</code>. تحدد الحقول <code>candidate_table</code> و<code>quarantine_table</code> و<code>approved_table</code> و<code>reports</code> مواقع البيانات والتقارير نسبةً إلى <code>WORK</code>.</p><p>داخل مجلد التقارير المحدد، يحتوي كل من <code>gx_trusted/</code> و<code>gx_mixed/</code> و<code>gx_rechecked/</code> على <code>checkpoint_result.json</code> ومشروع GX. استخدم مسارات <code>data_docs</code> داخل <code>gx_results</code> نسبةً إلى مجلد GX لتلك المرحلة لفتح <code>index.html</code> المولد. احتفظ بالمجلد كاملًا لسلامة الصفحات والملفات المرتبطة.</p><p>فسّر <code>distribution</code> و<code>scenario_freshness</code> وفق <a href="../../OBSERVABILITY.md">دليل المراقبة</a>. تواريخ السيناريو مدخلات تدريب تاريخية ثابتة، وليست قياسًا لزمن وصول حي.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>8. Save the approved state</h2><p>Record the reconciliation <code>82 = 75 + 7</code>, reasons and approval decision in <code>LAB06_NOTES.md</code>. Update your existing governance notes using <a href="../../GOVERNANCE.md">the guide</a>. Then run the fourth code cell and follow <a href="../../COMPLETION.md">the handoff checklist</a>.</p><p>A failed mixed-candidate result is expected; an unexpected exception or missing Data Docs is not. Preserve errors and prior outputs. Rerunning the full quality cell creates a separate candidate, quarantine and validation context rather than repairing old evidence in place.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٨. احفظ الحالة المعتمدة</h2><p>سجل المطابقة <code>82 = 75 + 7</code> والأسباب وقرار الاعتماد في <code>LAB06_NOTES.md</code>. حدّث ملاحظات الحوكمة القائمة وفق <a href="../../GOVERNANCE.md">الدليل</a>. ثم شغّل الخلية البرمجية الرابعة واتبع <a href="../../COMPLETION.md">قائمة الانتقال</a>.</p><p>فشل المرشح المختلط متوقع، لكن الاستثناء غير المتوقع أو غياب Data Docs ليس نجاحًا. احتفظ بالأخطاء والمخرجات السابقة. تُنشئ إعادة خلية الجودة كاملةً مرشحًا وعزلًا وسياق فحص مستقلًا، ولا تعدّل الأدلة القديمة لإظهارها ناجحة.</p></td></tr></table>
