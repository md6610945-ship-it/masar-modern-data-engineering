<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Lab 05 · Follow one event through the pipeline</h1><p><a href="../../README.md">Day 4</a> · <a href="../../STUDENT.ipynb">Student notebook</a> · <a href="../../STREAM_CONTRACT.md">Event contract</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اللاب 05 · تتبّع الحدث عبر خط البيانات</h1><p><a href="../../README.md">اليوم الرابع</a> · <a href="../../STUDENT.ipynb">دفتر الطالب</a> · <a href="../../STREAM_CONTRACT.md">عقد الأحداث</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>1. Prepare and open your existing workspace</h2><p>Start Kafka using <a href="../../SETUP.md">the setup guide</a>. Run the notebook's first code cell. <code>Continue workspace:</code> identifies your existing project folder. The streaming cell also verifies the Day 3 report and the corrected 75-trip Silver contents.</p><p>Inspect the three GPS source files without editing them: 216 base messages, two repeated events and one new late event. The optional reference notebook is not a prerequisite. The main notebook is your execution route.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>١. جهّز وافتح مساحة العمل القائمة</h2><p>ابدأ Kafka وفق <a href="../../SETUP.md">دليل الإعداد</a>، ثم شغّل أول خلية برمجية. تحدد رسالة <code>Continue workspace:</code> مجلد مشروعك القائم. تفحص خلية التدفق أيضًا تقرير اليوم الثالث ومحتوى Silver المصححة برحلاتها الـ75.</p><p>افحص ملفات المواقع الثلاثة دون تعديل: 216 رسالة أساسية وحدثان معاد إرسالهما وحدث جديد متأخر. الدفتر المرجعي الاختياري ليس متطلبًا سابقًا؛ مسار التنفيذ هو دفتر الطالب الأساسي.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>2. Run the 05 cell once</h2><p>The second code cell calls <code>run_stream_lab(spark, SOURCE, WORK)</code> and executes all four phases below. Do not create separate send commands between cells. Read the shared <a href="../../../src/masar/streaming.py">streaming.py</a> functions to follow the implementation without copying them.</p><p>Each full invocation creates a topic named <code>masar-day04-&lt;run_id&gt;</code>, two partitions and a new destination/checkpoint pair. Existing topics are not deleted or reused silently.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٢. شغّل خلية 05 مرة واحدة</h2><p>تستدعي الخلية البرمجية الثانية <code>run_stream_lab(spark, SOURCE, WORK)</code> وتنفذ المراحل الأربع أدناه. لا تُنشئ أوامر إرسال منفصلة بين الخلايا. اقرأ دوال <a href="../../../src/masar/streaming.py">streaming.py</a> المشتركة لتتبع التنفيذ دون نسخها.</p><p>ينشئ كل استدعاء كامل موضوعًا باسم <code>masar-day04-&lt;run_id&gt;</code> بقسمين، ووجهة ونقطة تحقق جديدتين. لا تُحذف الموضوعات السابقة أو يُعاد استخدامها تلقائيًا.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>3. Base: send and consume the original messages</h2><p><code>publish_fixture</code> records the sending intent before publishing each unchanged JSON line, with <code>trip_id</code> as its message key. It stores the acknowledged topic, partition and offset for each send. Producer retry protection does not eliminate deliberate new sends of the same event.</p><p><code>consume_available</code> uses Spark's Kafka source and a Delta append sink. It saves <code>raw_json</code>, the transport key, message key, headers, broker timestamp and ingestion time. <code>availableNow=True</code> drains available input and ends this phase; the configured limit is 72 offsets per trigger. Base counts are <strong>216 transport rows and 216 distinct events</strong>. [<a href="../../SOURCES.md">S1–S3, S5</a>]</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٣. الأساس: أرسل الرسائل الأصلية واستهلكها</h2><p>تسجل <code>publish_fixture</code> محاولة الإرسال قبل نشر كل سطر JSON دون تغيير، مع <code>trip_id</code> مفتاحًا للرسالة. تحفظ الموضوع والقسم والإزاحة المؤكدة لكل إرسال. حماية إعادة محاولة المنتج لا تزيل الإرسالات الجديدة المقصودة للحدث نفسه.</p><p>تستخدم <code>consume_available</code> مصدر Kafka في Spark ووجهة Delta بنمط الإلحاق. تحفظ <code>raw_json</code> ومفتاح الوصول ومفتاح الرسالة والترويسات وتوقيت الوسيط ووقت الاستقبال. تستهلك <code>availableNow=True</code> المدخلات المتاحة ثم تنهي المرحلة؛ وحد الإزاحات المحدد لكل تشغيل مصغر هو 72. نتيجة الأساس <strong>216 صف وصول و216 حدثًا مختلفًا</strong>. [<a href="../../SOURCES.md">S1–S3 وS5</a>]</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>4. Restart: resume without sending</h2><p>The helper starts a new execution of the same query with the same checkpoint and Delta destination, without publishing new messages. Counts remain <strong>216 / 216</strong>. Compare <code>query_id</code>, <code>query_run_id</code> and <code>checkpoint</code>: query identity and checkpoint remain the same; execution identity changes.</p><p>The restart occurs inside this one notebook cell. Rerunning the whole cell creates another full experiment. Do not delete the checkpoint or alter starting offsets to force the expected result. This is a clean query-resume test, not a broker crash or failover test. [<a href="../../SOURCES.md">S1, S2</a>]</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٤. الاستئناف: تابع دون إرسال جديد</h2><p>تبدأ الدالة تنفيذًا جديدًا للاستعلام نفسه بنقطة التحقق ووجهة Delta نفسيهما، دون نشر رسائل جديدة. يبقى العدد <strong>216 / 216</strong>. قارن <code>query_id</code> و<code>query_run_id</code> و<code>checkpoint</code>: تبقى هوية الاستعلام والنقطة ثابتتين، وتتغير هوية التنفيذ.</p><p>يحدث اختبار الاستئناف داخل خلية الدفتر نفسها. تعيد الخلية الكاملة إنشاء تجربة أخرى عند تشغيلها مجددًا. لا تحذف نقطة التحقق أو تغيّر إزاحات البداية لفرض النتيجة. هذا اختبار استئناف نظيف للاستعلام، وليس اختبار تعطل الوسيط أو الانتقال إلى وسيط بديل. [<a href="../../SOURCES.md">S1 وS2</a>]</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>5. Replay, then receive the late event</h2><p>The replay file sends two existing observations again. Transport rows increase to <strong>218</strong>, but distinct events remain <strong>216</strong>. The late file then adds the new <code>SYN_E_LATE001</code> observation: <strong>219 transport rows and 217 distinct events</strong>.</p><p>An old event timestamp does not make that new event a duplicate. The lab keeps it; it does not execute a watermark experiment. The notebook prints:<br><code>Transport rows: [216, 216, 218, 219]</code><br><code>Unique event IDs: [216, 216, 216, 217]</code></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٥. أعد الإرسال ثم استقبل الحدث المتأخر</h2><p>يعيد ملف الإعادة إرسال ملاحظتين موجودتين. ترتفع صفوف الوصول إلى <strong>218</strong>، وتبقى الأحداث المختلفة <strong>216</strong>. ثم يضيف الملف المتأخر الحدث الجديد <code>SYN_E_LATE001</code>، فتكون النتيجة <strong>219 صف وصول و217 حدثًا مختلفًا</strong>.</p><p>لا يجعل توقيت الحدث القديم الحدث الجديد مكررًا. يحتفظ به اللاب، ولا ينفذ تجربة Watermark. يطبع الدفتر:<br><code>Transport rows: [216, 216, 218, 219]</code><br><code>Unique event IDs: [216, 216, 216, 217]</code></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>6. Reconcile content as well as counts</h2><p>The 13 checks compare acknowledged transport keys with consumed keys, exact source JSON lines with stored lines, query identities and checkpoint files, and distinct events with the source-derived expected content. They also verify that every event links to a trusted trip.</p><p>Conflicting payloads with one <code>event_id</code> are rejected before deduplication. After all phases, a batch read of raw Delta data is deduplicated into a new 217-event Delta snapshot. This is not a stateful streaming deduplication operator. Day 1 GPS ingestion is separate; do not combine it with this stream and count the same base events twice.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٦. طابق المحتوى والعدد معًا</h2><p>تقارن الفحوص الـ13 مفاتيح الوصول المؤكدة بالمستهلكة، وأسطر JSON الأصلية بالمحفوظة، وهويات الاستعلام وملفات نقطة التحقق، والأحداث المختلفة بالمحتوى المتوقع من المصدر. وتتحقق من ارتباط كل حدث برحلة معتمدة.</p><p>يُرفض اختلاف محتوى حدثين يحملان <code>event_id</code> نفسه قبل إزالة التكرار. بعد المراحل، تُقرأ بيانات Delta الخام كدفعة وتُختصر إلى لقطة Delta جديدة من 217 حدثًا. ليست هذه عملية إزالة تكرار تحتفظ بالحالة داخل التدفق. استقبال المواقع في اليوم الأول منفصل؛ لا تجمعه مع هذا التدفق وتحتسب الأحداث الأساسية مرتين.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>7. Find your evidence and continue</h2><p>Open <code>WORK/reports/day04_stream_latest.json</code>. Its <code>raw_table</code> and <code>event_table</code> fields are relative to <code>WORK</code>. Under <code>reports/day04/&lt;run_id&gt;/</code>, retain the three producer receipts, four <code>consumer_*.json</code> reports and <code>streaming.json</code>. The checkpoint is under <code>checkpoints/day04/gps_&lt;run_id&gt;/</code>.</p><p>Explain the four phases in <code>LAB05_NOTES.md</code>. Keep interrupted attempts and their messages; a partial acknowledgment is not proof of complete delivery. Continue to <a href="../lab06/WALKTHROUGH.md">Lab 06</a>, then save using <a href="../../COMPLETION.md">the daily checklist</a>.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٧. حدد أدلتك ثم تابع</h2><p>افتح <code>WORK/reports/day04_stream_latest.json</code>. حقلا <code>raw_table</code> و<code>event_table</code> مساران نسبيان إلى <code>WORK</code>. احتفظ داخل <code>reports/day04/&lt;run_id&gt;/</code> بتأكيدات المنتج الثلاثة وتقارير <code>consumer_*.json</code> الأربعة و<code>streaming.json</code>. تقع نقطة التحقق تحت <code>checkpoints/day04/gps_&lt;run_id&gt;/</code>.</p><p>فسّر المراحل الأربع في <code>LAB05_NOTES.md</code>. احتفظ بالمحاولات المتوقفة ورسائلها؛ تأكيد جزء من الإرسال لا يثبت وصول الدفعة كاملةً. تابع <a href="../lab06/WALKTHROUGH.md">اللاب 06</a>، ثم احفظ وفق <a href="../../COMPLETION.md">قائمة اليوم</a>.</p></td></tr></table>
