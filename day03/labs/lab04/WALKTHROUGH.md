<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Lab 04 · Correct, inspect and recover</h1><p><a href="../../README.md">Day 3</a> · <strong><a href="../../STUDENT.ipynb">Student notebook</a></strong> · <a href="../../CHANGE_POLICY.md">Change rules</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اللاب 04 · صحّح وافحص واستعد</h1><p><a href="../../README.md">اليوم الثالث</a> · <strong><a href="../../STUDENT.ipynb">دفتر الطالب</a></strong> · <a href="../../CHANGE_POLICY.md">قواعد التغيير</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>1. Open the Day 2 workspace</h2><p>Run the first code cell in <code>day03/STUDENT.ipynb</code>. The printed <code>Continue workspace:</code> identifies the existing workspace. The next cell also checks the Day 2 Silver report and actual table contents.</p><p>The starting table has 75 unique trips and SAR 1,875.60 in total fares. Restore your own Day 2 handoff at the repository root when changing sessions. A pointer without its Delta folders is incomplete. Do not rebuild Silver from the optional reference notebook.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>١. افتح مساحة عمل اليوم الثاني</h2><p>شغّل أول خلية برمجية في <code>day03/STUDENT.ipynb</code>. تحدد رسالة <code>Continue workspace:</code> مساحة العمل القائمة. وتفحص الخلية التالية تقرير Silver لليوم الثاني ومحتويات الجدول الفعلية.</p><p>يبدأ الجدول بـ75 رحلة فريدة وإجمالي أجور 1,875.60 ريالًا. عند تغيير الجلسة، استعد ملف انتقال اليوم الثاني الخاص بك في جذر المستودع. المؤشر دون مجلدات Delta لا يكفي. لا تُعد بناء Silver من الدفتر المرجعي الاختياري.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>2. Run 04a and read the correction</h2><p>Run the second code cell, which calls <code>run_transactions_lab</code>. It reads the existing Delta table, records its current version, and lands <code>correction.csv</code> as a separate Bronze receipt. The scenario assigns source revision 2; that revision is not a field invented inside the original CSV.</p><p>For <code>SYN_T0001</code>, the fare changes from <strong>SAR 18.00 to SAR 23.00</strong>. The table still contains <strong>75 trips</strong>; its total rises by SAR 5.00 to <strong>SAR 1,880.60</strong>. Inspect <code>before</code> and <code>after_correction</code> in the printed result. These objects show actual versions, schema, row counts and business-content hashes.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٢. شغّل 04a واقرأ التصحيح</h2><p>شغّل الخلية البرمجية الثانية التي تستدعي <code>run_transactions_lab</code>. تقرأ جدول Delta القائم وتسجل نسخته الحالية وتحفظ <code>correction.csv</code> باستقبال Bronze منفصل. يحدد السيناريو مراجعة المصدر رقم 2؛ وليست حقلًا أُضيف إلى ملف CSV الأصلي.</p><p>تتغير أجرة <code>SYN_T0001</code> من <strong>18.00 إلى 23.00 ريالًا</strong>. يبقى الجدول عند <strong>75 رحلة</strong> ويرتفع إجماليه 5.00 ريالات ليصبح <strong>1,880.60 ريالًا</strong>. افحص <code>before</code> و<code>after_correction</code> في النتيجة المطبوعة؛ فهما يعرضان النسخ والمخطط وأعداد الصفوف وبصمات محتوى الأعمال الفعلية.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>3. Understand the three replay decisions</h2><p>The same 04a cell tests these cases:<br><strong>Same correction again:</strong> retain SAR 23.00 and 75 trips.<br><strong>Older revision arrives:</strong> ignore the old SAR 18.00 value.<br><strong>Same revision, conflicting fare:</strong> reject the transient SAR 24.00 probe before MERGE.</p><p>The shared <a href="../../../src/masar/delta_lab.py">revision_merge_delta function</a> enforces this application policy and checks source/target key uniqueness. Delta does not automatically know which business revision is authoritative. Compare <code>after_correction</code>, <code>after_replay</code> and <code>after_stale_replay</code> in <code>reports/day03_transactions.json</code>; their business-content hashes must agree. Physical versions need not match.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٣. افهم قرارات الإعادة الثلاثة</h2><p>تختبر خلية 04a نفسها الحالات التالية:<br><strong>التصحيح نفسه مرة أخرى:</strong> تبقى الأجرة 23.00 ريالًا والعدد 75 رحلة.<br><strong>وصول مراجعة أقدم:</strong> تُتجاهل الأجرة القديمة 18.00 ريالًا.<br><strong>المراجعة نفسها بأجرة متعارضة:</strong> يُرفض صف الاختبار المؤقت ذي الأجرة 24.00 ريالًا قبل MERGE.</p><p>تطبق <a href="../../../src/masar/delta_lab.py">الدالة المشتركة revision_merge_delta</a> سياسة التطبيق هذه وتفحص تفرد مفاتيح المصدر والهدف. لا تعرف Delta تلقائيًا أي مراجعة أعمال يجب اعتمادها. قارن <code>after_correction</code> و<code>after_replay</code> و<code>after_stale_replay</code> داخل <code>reports/day03_transactions.json</code>؛ يجب تطابق بصمات محتواها. لا يلزم تطابق أرقام نسخ الجدول.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>4. Read the past and inspect a rejected write</h2><p>04a reads the version recorded before correction using <code>versionAsOf</code>. The earlier row has the SAR 18.00 fare; current Silver has SAR 23.00. Reading history does not replace the current table.</p><p>It then creates an independent copy with fare and key-presence constraints. One append contains a valid probe and the negative-fare case <code>SYN_BAD002</code>. The expected constraint violation rejects the whole write. Check <code>constraint_probe.committed_table_unchanged</code> and the before/after state in the report.</p><p>An expected constraint message can appear in logs while the cell completes successfully. The intended rejection and unchanged committed table are the evidence; an unrelated exception or an uncaught notebook error is not a pass.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٤. اقرأ الماضي وافحص كتابة مرفوضة</h2><p>يقرأ 04a النسخة المسجلة قبل التصحيح باستخدام <code>versionAsOf</code>. تعرض الرحلة في النسخة القديمة أجرة 18.00 ريالًا، وفي Silver الحالية 23.00 ريالًا. قراءة التاريخ لا تستبدل الجدول الحالي.</p><p>ثم ينشئ نسخة مستقلة عليها قيدا الأجرة ووجود المفتاح. تحتوي كتابة واحدة على صف اختبار صحيح وحالة الأجرة السالبة <code>SYN_BAD002</code>. يرفض القيد الكتابة كاملةً. افحص <code>constraint_probe.committed_table_unchanged</code> وحالة الجدول قبل المحاولة وبعدها داخل التقرير.</p><p>قد تظهر رسالة القيد المتوقعة في السجل مع نجاح الخلية. الدليل هو الرفض المقصود وثبات الجدول المعتمد؛ الاستثناء غير المرتبط أو خطأ الدفتر غير المعالج ليس نجاحًا.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>5. Run 04b: change a copy’s schema</h2><p>Run the third code cell, which calls <code>run_maintenance_lab</code> after 04a passes. It creates a new copy containing the other 74 trips, excluding <code>SYN_T0002</code>. This avoids duplicating an existing trip when its extended row is added.</p><p>The first append of <code>schema_change.csv</code> is rejected because <code>surcharge_sar</code> is unexpected. A second append explicitly allows that field for this write with <code>mergeSchema=true</code>. Inspect the maintenance report: <strong>75 unique trips, 74 null surcharges and one surcharge of SAR 2.00</strong>.</p><p>The surcharge is a separate field, not a change to <code>fare_sar</code>. The original Silver schema and its corrected fares remain unchanged. Do not enable automatic schema evolution for the whole session.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٥. شغّل 04b: غيّر مخطط نسخة معزولة</h2><p>شغّل الخلية البرمجية الثالثة التي تستدعي <code>run_maintenance_lab</code> بعد نجاح 04a. تنشئ نسخة جديدة بالرحلات الـ74 الأخرى مع استبعاد <code>SYN_T0002</code>، حتى لا تتكرر الرحلة عند إضافة صفها الموسع.</p><p>تُرفض الإضافة الأولى من <code>schema_change.csv</code> لأن الحقل <code>surcharge_sar</code> غير متوقع. تسمح الإضافة الثانية بالحقل لهذه الكتابة فقط عبر <code>mergeSchema=true</code>. افحص تقرير الصيانة: <strong>75 رحلة فريدة و74 رسمًا إضافيًا فارغًا ورسم واحد بقيمة 2.00 ريال</strong>.</p><p>الرسم الإضافي حقل منفصل، وليس تعديلًا لـ<code>fare_sar</code>. يبقى مخطط Silver الأصلي وأجوره المصححة دون تغيير. لا تفعّل تطوير المخطط التلقائي للجلسة كاملةً.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>6. Inspect compaction and recovery</h2><p>In a separate compaction copy, 04b records <code>numFiles</code> and <code>sizeInBytes</code> before and after <code>OPTIMIZE</code>. It checks that business values are unchanged. Read the <code>optimize</code> object in the report; record what happened rather than claiming a guaranteed speedup.</p><p>A different recovery copy follows <strong>75 → 74 → 75 rows</strong>: create the copy, delete <code>SYN_T0001</code>, then restore the pre-delete state. Inspect the printed <code>recovery</code> object and history. RESTORE creates a later commit; it does not turn the version number backward. These operations do not delete a row from the continuing Silver table.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٦. افحص تجميع الملفات والاستعادة</h2><p>في نسخة مستقلة لتجميع الملفات، يسجل 04b قيمتي <code>numFiles</code> و<code>sizeInBytes</code> قبل <code>OPTIMIZE</code> وبعده، ويفحص ثبات قيم الأعمال. اقرأ كائن <code>optimize</code> داخل التقرير وسجل ما حدث فعلًا دون افتراض تحسن مضمون في السرعة.</p><p>تمر نسخة استعادة أخرى بتسلسل <strong>75 ← 74 ← 75 صفًا</strong>: إنشاء النسخة ثم حذف <code>SYN_T0001</code> ثم استعادة الحالة السابقة للحذف. افحص كائن <code>recovery</code> المطبوع والسجل. ينشئ RESTORE معاملة أحدث، ولا يعيد رقم النسخة إلى الخلف. لا تحذف هذه العمليات صفًا من جدول Silver الذي ستكمل عليه.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>7. Preview cleanup only</h2><p>The final maintenance operation is <code>VACUUM ... RETAIN 168 HOURS DRY RUN</code> on the recovery copy. Check the printed <code>vacuum</code> object: <code>dry_run_only</code> is true and <code>files_deleted_by_vacuum</code> is zero. An empty candidate list is expected for newly created files.</p><p>Keep retention safeguards enabled. Do not remove Parquet files or edit <code>_delta_log</code> manually. Actual cleanup can remove files needed to read or restore older versions; it is outside this exercise. See <a href="../../SOURCES.md">D5 and D8</a>.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٧. عاين التنظيف فقط</h2><p>آخر عملية صيانة هي <code>VACUUM ... RETAIN 168 HOURS DRY RUN</code> على نسخة الاستعادة. افحص كائن <code>vacuum</code> المطبوع: تكون <code>dry_run_only</code> صحيحة وتكون <code>files_deleted_by_vacuum</code> صفرًا. القائمة الفارغة متوقعة للملفات المنشأة حديثًا.</p><p>أبقِ وسائل حماية الاحتفاظ مفعّلة. لا تحذف ملفات Parquet أو تعدّل <code>_delta_log</code> يدويًا. قد يحذف التنظيف الفعلي ملفات لازمة لقراءة نسخة قديمة أو استعادتها؛ وهو خارج هذا التمرين. راجع <a href="../../SOURCES.md">D5 وD8</a>.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>8. Save evidence and continue</h2><p>Read <code>reports/day03_transactions.json</code> and <code>reports/day03_maintenance_latest.json</code> under the printed <code>WORK</code> path. Record your explanation in <code>LAB04_NOTES.md</code>; use <a href="../../PRACTICE.md">the questions</a> as prompts.</p><p>Run the fourth and final code cell to create <code>outputs/day03_handoff.zip</code>. Keep the executed notebook and notes separately. Retain the Day 2 handoff for its separate dbt reports and documentation; the Day 3 archive preserves the continuing workspace, not that isolated dbt workspace.</p><p>If 04a has already completed, rerunning it verifies the current corrected table and uses its saved report; it does not repeat every historical test. 04b makes fresh copies on each run. For a completely fresh experiment, keep the old workspace and repeat the earlier days in a new workspace. Follow <a href="../../COMPLETION.md">the completion checklist</a>.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٨. احفظ الأدلة ثم تابع</h2><p>اقرأ <code>reports/day03_transactions.json</code> و<code>reports/day03_maintenance_latest.json</code> تحت مسار <code>WORK</code> المطبوع. سجل تفسيرك في <code>LAB04_NOTES.md</code> واستخدم <a href="../../PRACTICE.md">الأسئلة</a> لتوجيه ملاحظاتك.</p><p>شغّل الخلية البرمجية الرابعة والأخيرة لإنشاء <code>outputs/day03_handoff.zip</code>. احتفظ بالدفتر المنفذ والملاحظات بصورة منفصلة. احتفظ أيضًا بملف انتقال اليوم الثاني لتقارير dbt وتوثيقه؛ فملف اليوم الثالث يحفظ مساحة العمل المستمرة، لا مساحة dbt المعزولة.</p><p>إذا اكتمل 04a سابقًا، فإن إعادته تفحص الجدول المصحح الحالي وتستخدم تقريره المحفوظ، ولا تكرر جميع الاختبارات التاريخية. ينشئ 04b نسخًا جديدة عند كل تشغيل. لتجربة جديدة بالكامل، احتفظ بالمساحة القديمة وأعد الأيام السابقة في مساحة جديدة. اتبع <a href="../../COMPLETION.md">قائمة الاكتمال</a>.</p></td></tr></table>
