<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Lab 08 · Read, reconcile and export</h1><p><a href="../../README.md">Day 5</a> · <strong><a href="../../STUDENT.ipynb">Student notebook</a></strong> · <a href="../../AI_BI_GUIDE.md">AI/BI guide</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اللاب 08 · اقرأ وطابق وصدّر</h1><p><a href="../../README.md">اليوم الخامس</a> · <strong><a href="../../STUDENT.ipynb">دفتر الطالب</a></strong> · <a href="../../AI_BI_GUIDE.md">دليل AI وBI</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>1. Continue after Lab 07</h2><p>Run the third code cell, labelled <strong>08</strong>, after the recovery cell has passed. It calls <code>run_serving_lab</code>, checks the result, prints the reporting and AI examples, and stops its Spark session.</p><p>This cell performs the steps below automatically. The <a href="../../../src/masar/serving.py">shared implementation</a> is the code to inspect; do not copy it into a parallel solution. No running Kafka broker is needed to read these completed Delta outputs.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>١. تابع بعد اللاب 07</h2><p>شغّل الخلية البرمجية الثالثة ذات العنوان <strong>08</strong> بعد نجاح خلية التعافي. تستدعي <code>run_serving_lab</code> وتفحص النتيجة وتطبع أمثلة التقارير وAI ثم توقف جلسة Spark.</p><p>تنفذ هذه الخلية الخطوات التالية تلقائيًا. افحص <a href="../../../src/masar/serving.py">التنفيذ المشترك</a> دون نسخه إلى حل موازٍ. لا يلزم تشغيل وسيط Kafka لقراءة مخرجات Delta المكتملة هذه.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>2. Select the complete release</h2><p><code>read_release</code> follows <code>reports/day05_gold_latest.json</code>, checks the manifest hash and release ID, requires exactly the eight expected tables, and reads their recorded Delta versions. It compares actual row counts and logical contents with the manifest.</p><p>Do not read every release folder and combine the files: the failed candidate remains beside good releases for diagnosis. Product names such as <code>bi.fact_trips</code> map to paths in the manifest; they are not automatically available as named SQL tables in a new session.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٢. اختر الإصدار المكتمل</h2><p>تتبع <code>read_release</code> ملف <code>reports/day05_gold_latest.json</code> وتفحص بصمة سجل الإصدار ومعرفه، وتشترط الجداول الثمانية المتوقعة تحديدًا، وتقرأ نسخ Delta المسجلة لها. تقارن الأعداد والمحتوى الفعليين بالسجل.</p><p>لا تقرأ كل مجلدات الإصدارات وتجمع ملفاتها؛ تبقى المحاولة الفاشلة بجوار الإصدارات السليمة للتشخيص. تقابل أسماء مثل <code>bi.fact_trips</code> مسارات في السجل، ولا تصبح جداول SQL مسماة تلقائيًا في جلسة جديدة.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>3. Check the exports</h2><p>The helper writes one CSV for each validated table under <code>reports/serving/&lt;export_id&gt;/</code>. It rereads each file, checks column names and row values, and records the row count and SHA-256. The <code>exports</code> object in <code>reports/day05_serving_latest.json</code> gives the exact paths.</p><p>For example, the fact file is <code>bi_fact_trips.csv</code>, and the labels file is <code>ai_zone_hourly_labels.csv</code>. Empty fields represent null; booleans are text. Keep <a href="../../DATA_PRODUCTS.md">the contracts</a> so the recipient can restore types and units correctly. [D5-6]</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٣. افحص الصادرات</h2><p>تكتب الدالة ملف CSV لكل جدول متحقق منه تحت <code>reports/serving/&lt;export_id&gt;/</code>. تعيد قراءة كل ملف وتفحص أسماء الأعمدة وقيم الصفوف وتسجل العدد وبصمة SHA-256. يحدد كائن <code>exports</code> في <code>reports/day05_serving_latest.json</code> المسارات الدقيقة.</p><p>مثلًا، ملف الوقائع <code>bi_fact_trips.csv</code> وملف النتائج المستقبلية <code>ai_zone_hourly_labels.csv</code>. تمثل الحقول الفارغة null، وتكتب القيم المنطقية نصيًا. احتفظ بـ<a href="../../DATA_PRODUCTS.md">تعريفات الحقول</a> ليعيد المستلم الأنواع والوحدات بصورة صحيحة. [D5-6]</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>4. Reconcile the Spark SQL summary</h2><p>The same call registers the pinned fact table as <code>masar_day05_fact</code> and executes <a href="../../sql/bi_zone_summary.sql">the supplied SQL file</a> while Spark is active. Inspect the printed <code>BI totals</code>: Dammam 25 / SAR 670.40, Jeddah 25 / SAR 625.20, Riyadh 25 / SAR 585.00.</p><p>Confirm the grand total of 75 trips and SAR 1,880.60. The fact's event counts sum to 217 because events were aggregated per trip before joining. The query is already executed by the cell; opening the SQL file is for reading, not an instruction to rerun it after the cell has stopped Spark.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٤. طابق ملخص Spark SQL</h2><p>يسجل الاستدعاء نفسه نسخة الوقائع باسم <code>masar_day05_fact</code> ويشغّل <a href="../../sql/bi_zone_summary.sql">ملف SQL المرفق</a> أثناء عمل Spark. افحص <code>BI totals</code> المطبوع: الدمام 25 رحلة / 670.40 ريالًا، وجدة 25 / 625.20، والرياض 25 / 585.00.</p><p>تحقق من إجمالي 75 رحلة و1,880.60 ريالًا. يبلغ مجموع أعداد الأحداث في الوقائع 217 لأن الأحداث جُمعت حسب الرحلة قبل الربط. تنفذ الخلية الاستعلام بالفعل؛ فتح ملف SQL للقراءة، وليس توجيهًا لإعادته بعد إيقاف Spark في نهاية الخلية.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>5. Interpret the feature and label examples</h2><p>The notebook prints one row from each AI table. At <code>as_of_utc=2026-06-04T03:05:00Z</code>, the Dammam example has eight completed trips and a historical mean duration of 1,470.00 seconds. Its <code>prediction_hour_utc</code> is 04:00 UTC; it is not a prediction value.</p><p>The corresponding label example has <code>target_trip_count=None</code> and <code>label_status=UNOBSERVED</code>. Inspect the two CSV exports for all three city rows. Refer to <a href="../../AI_BI_GUIDE.md">the cutoff, availability rules and four-field allowlist</a>. No model is trained or evaluated.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٥. فسّر مثالي الخصائص والنتائج</h2><p>يطبع الدفتر صفًا واحدًا من كل جدول AI. عند <code>as_of_utc=2026-06-04T03:05:00Z</code>، يحتوي مثال الدمام ثماني رحلات مكتملة ومتوسط مدة تاريخي 1,470.00 ثانية. قيمة <code>prediction_hour_utc</code> هي 04:00 UTC؛ وليست قيمة تنبؤ.</p><p>يحمل مثال النتيجة المقابل <code>target_trip_count=None</code> و<code>label_status=UNOBSERVED</code>. افحص ملفي CSV لصفوف المدن الثلاثة. راجع <a href="../../AI_BI_GUIDE.md">وقت القطع وقواعد الإتاحة وقائمة المدخلات الأربعة</a>. لا يُدرّب نموذج أو يُقيّم.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>6. Check the report and archive</h2><p>All 16 printed serving checks must pass. Open <code>reports/day05_serving_latest.json</code> and confirm that <code>source_release_id</code> identifies the selected successful release. Retain its export paths and hashes with the report.</p><p>Run the fourth code cell to create <code>outputs/day05_handoff.zip</code>. It includes the pointer and current workspace; keep the executed notebooks, notes and earlier separate output archives as well. If you rebuild Lab 07, run Lab 08 again before recreating the final archive.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٦. افحص التقرير والأرشيف</h2><p>يجب نجاح فحوص التقديم المطبوعة الـ16. افتح <code>reports/day05_serving_latest.json</code> وتحقق أن <code>source_release_id</code> يحدد الإصدار السليم المختار. احتفظ بمسارات الصادرات وبصماتها مع التقرير.</p><p>شغّل الخلية البرمجية الرابعة لإنشاء <code>outputs/day05_handoff.zip</code>. يشمل المؤشر ومساحة العمل الحالية؛ واحتفظ أيضًا بالدفاتر المنفذة والملاحظات وأرشيفات المخرجات المنفصلة السابقة. إذا أعدت بناء اللاب 07، فأعد اللاب 08 قبل إعادة إنشاء الأرشيف النهائي.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>7. Submit the work you have already built</h2><p>Answer questions 5–8 in <code>LAB08_NOTES.md</code>, use <a href="../../COMPLETION.md">the final-day checklist</a>, and follow <a href="../../../project/SUBMISSION.md">the shared submission guide</a>. Include the programme, SDAIA Academy, its link and <code>#SDAIAAcademy</code> in the stated places.</p><p>Do not add a separate model, dashboard or new dataset to satisfy this lab. Do not force generated output folders into Git. The optional inventory and all-days diagnostic tools are not extra passing conditions; their limited roles are explained in <a href="../../INTEGRATION.md">the integration guide</a>.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٧. سلّم العمل الذي بنيته بالفعل</h2><p>أجب عن الأسئلة 5–8 في <code>LAB08_NOTES.md</code> واستخدم <a href="../../COMPLETION.md">قائمة اليوم الأخير</a> واتبع <a href="../../../project/SUBMISSION.md">دليل التسليم المشترك</a>. أدرج البرنامج وأكاديمية سدايا ورابطها والوسم <code>#SDAIAAcademy</code> في المواضع المحددة.</p><p>لا تضف نموذجًا منفصلًا أو لوحة معلومات أو بيانات جديدة لإكمال اللاب. ولا تفرض إضافة مجلدات المخرجات المولدة إلى Git. أدوات الحصر والتشخيص الشامل الاختيارية ليست شروط نجاح إضافية؛ يوضح <a href="../../INTEGRATION.md">دليل التكامل</a> دورها المحدود.</p></td></tr></table>
