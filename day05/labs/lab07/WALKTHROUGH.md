<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Lab 07 · Follow the good, failed and recovered builds</h1><p><a href="../../README.md">Day 5</a> · <strong><a href="../../STUDENT.ipynb">Student notebook</a></strong> · <a href="../../DATA_PRODUCTS.md">Output definitions</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اللاب 07 · تتبع البناء السليم والفاشل والتعافي</h1><p><a href="../../README.md">اليوم الخامس</a> · <strong><a href="../../STUDENT.ipynb">دفتر الطالب</a></strong> · <a href="../../DATA_PRODUCTS.md">تعريفات المخرجات</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>1. Open the continuing workspace</h2><p>Run the first code cell in <code>day05/STUDENT.ipynb</code>. <code>Continue workspace:</code> identifies the existing <code>WORK</code> folder. On a new session, restore your own Day 4 handoff at the repository root before this step.</p><p>The pointer still has the Day 1 filename because the workspace is cumulative. Day 5 additionally needs the completed Day 4 reports and actual approved Delta tables; a Day 1-only workspace is not enough.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>١. افتح مساحة العمل المستمرة</h2><p>شغّل أول خلية برمجية في <code>day05/STUDENT.ipynb</code>. تحدد رسالة <code>Continue workspace:</code> مجلد <code>WORK</code> القائم. في جلسة جديدة، استعد ملف اليوم الرابع الخاص بك في جذر المستودع قبل هذه الخطوة.</p><p>يبقى اسم ملف المؤشر مرتبطًا باليوم الأول لأن مساحة العمل تراكمية. يحتاج اليوم الخامس أيضًا إلى تقارير اليوم الرابع المكتملة وجداول Delta المعتمدة الفعلية؛ فلا تكفي مساحة تضم اليوم الأول فقط.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>2. Run the Lab 07 cell once</h2><p>The second code cell calls <code>run_recovery_exercise</code> from <a href="../../../src/masar/serving.py">serving.py</a>. It starts Spark without Kafka, performs the sequence below, prints four checks and stops Spark. Wait for that cell to finish before Lab 08.</p><p>The remaining steps explain what this same call does and which reports to inspect. You do not need to invoke the helper functions individually or supply a new dataset.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٢. شغّل خلية اللاب 07 مرة واحدة</h2><p>تستدعي الخلية البرمجية الثانية <code>run_recovery_exercise</code> من <a href="../../../src/masar/serving.py">serving.py</a>. تبدأ Spark دون Kafka وتنفذ التسلسل التالي وتطبع أربعة فحوص ثم توقف Spark. أكمل هذه الخلية قبل اللاب 08.</p><p>تشرح الخطوات التالية ما يفعله الاستدعاء نفسه والتقارير التي تفحصها. لا تحتاج إلى استدعاء الدوال المساعدة منفردة أو توفير بيانات جديدة.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>3. Verify and pin the two inputs</h2><p><code>load_validated_inputs</code> checks the Day 4 quality and stream reports, their required checks and fixed dataset identity. It reads the quality-approved table and distinct-event table named by those reports, records each current Delta version, then reads that version explicitly.</p><p>The approved trip contents must match the corrected 75 trips, and the event snapshot must contain 217 distinct events matching the source. The successful release's <code>inputs</code> object records paths, versions, row counts and hashes of the two prerequisite reports. The quarantined candidate is not an input.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٣. افحص المدخلين وثبّت نسختيهما</h2><p>تفحص <code>load_validated_inputs</code> تقريري الجودة والتدفق لليوم الرابع وفحوصهما المطلوبة وهوية البيانات الثابتة. تقرأ جدول الرحلات المعتمد وجدول الأحداث المختلفة المحددين فيهما، وتسجل نسخة Delta الحالية لكل منهما ثم تقرأها صراحةً.</p><p>يجب أن يطابق محتوى الرحلات المعتمد الرحلات المصححة الـ75، وأن تحتوي لقطة الأحداث على 217 حدثًا مختلفًا مطابقًا للمصدر. يسجل كائن <code>inputs</code> في الإصدار السليم المسارات والنسخ والأعداد وبصمتي تقريري المتطلبات السابقة. لا تُستخدم الدفعة المعزولة كمدخل.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>4. Trace the Spark transformations</h2><p><code>build_frames</code> maps cities to explicit city proxies, counts distinct events per trip before joining, builds two Gold summaries and three dimensions plus the fact table, and prepares separate feature and label tables. See <a href="../../DATA_PRODUCTS.md">all eight contracts</a>.</p><p>Spark builds the fact rows and aggregates from actual Delta inputs. A separate source-derived calculation checks the results; it does not supply substitute rows to the native tables. Keep the one-trip grain when checking fares.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٤. تتبع تحويلات Spark</h2><p>تربط <code>build_frames</code> المدن بتمثيلاتها الصريحة، وتعد الأحداث المختلفة لكل رحلة قبل الربط، وتبني ملخصي Gold وثلاثة أبعاد وجدول الوقائع، ثم جدولين منفصلين للخصائص والنتائج المستقبلية. راجع <a href="../../DATA_PRODUCTS.md">التعريفات الثمانية</a>.</p><p>يبني Spark الوقائع والتجميعات من مدخلات Delta الفعلية. يفحص حساب منفصل مشتق من المصدر النتائج، ولا يزوّد الجداول الأصلية بصفوف بديلة. حافظ على معنى الصف «رحلة واحدة» عند فحص الأجور.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>5. Select the first good release</h2><p>Each table is written with <code>errorifexists</code> under a new release ID. The helper rereads Delta, checks schema and keys, and compares the canonical business contents. After all eight pass, it saves <code>mini_lakehouse/serving_releases/&lt;release_id&gt;/release.json</code> and updates <code>reports/day05_gold_latest.json</code>.</p><p>The manifest records each table's path, version, row count and content hash. Updating this local pointer means the supplied reader can select a complete release; it is not a global multi-table transaction. [D5-1, D5-2]</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٥. اختر الإصدار السليم الأول</h2><p>يُكتب كل جدول بوضع <code>errorifexists</code> تحت معرف إصدار جديد. تعيد الدالة القراءة من Delta وتفحص المخطط والمفاتيح وتقارن محتوى الأعمال المعياري. بعد نجاح الجداول الثمانية تحفظ <code>mini_lakehouse/serving_releases/&lt;release_id&gt;/release.json</code> وتحدث <code>reports/day05_gold_latest.json</code>.</p><p>يسجل ملف الإصدار مسار كل جدول ونسخته وعدد صفوفه وبصمة محتواه. يعني تحديث المؤشر المحلي أن القارئ المرفق يستطيع اختيار إصدار مكتمل، وليس اعتماد معاملة شاملة للجداول كلها. [D5-1، D5-2]</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>6. Inspect the intentional failure</h2><p>A second candidate fails after writing its first table. Only the deliberate <code>InjectedFailure</code> is handled as expected. The helper checks that the previous pointer bytes are unchanged and rereads the former good release successfully.</p><p>Find the failure record under <code>reports/day05_failures/&lt;candidate_id&gt;.json</code>. It should identify the written table, the deliberate error, no completed release manifest and preservation of the previous pointer. Partial table files remain for diagnosis. Their existence is not a complete release, and an unrelated exception must not be ignored.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٦. افحص الفشل المقصود</h2><p>تفشل نسخة مرشحة ثانية بعد كتابة أول جدول فيها. يُعالج <code>InjectedFailure</code> المقصود فقط بوصفه متوقعًا. تفحص الدالة ثبات ملف المؤشر السابق وتعيد قراءة الإصدار السليم السابق بنجاح.</p><p>ابحث عن سجل الفشل تحت <code>reports/day05_failures/&lt;candidate_id&gt;.json</code>. يحدد الجدول المكتوب والخطأ المقصود وعدم وجود سجل إصدار مكتمل والحفاظ على المؤشر السابق. تبقى الملفات الجزئية للتشخيص؛ وجودها ليس إصدارًا كاملًا، ولا يجوز تجاهل استثناء غير مرتبط.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>7. Check recovery and continue</h2><p>A third build succeeds under a new ID. All eight logical-content hashes must match the first good release. Read <code>reports/day05_recovery.json</code>: <code>run_ids</code> contains the two successful IDs; all four <code>checks</code> must be true. The newest good release is now selected.</p><p>This is controlled recovery within one Spark session, not a simulated machine crash. In <code>LAB07_NOTES.md</code>, answer questions 1–4 from <a href="../../PRACTICE.md">the practice page</a>, then run <a href="../lab08/WALKTHROUGH.md">Lab 08</a>. Do not delete the failed candidate or manually move the pointer to it. <a href="../../INTEGRATION.md">Optional diagnostics</a> are separate from this required route.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٧. تحقق من التعافي ثم تابع</h2><p>ينجح بناء ثالث بمعرف جديد. يجب أن تطابق بصمات محتوى الجداول الثمانية الإصدار السليم الأول. اقرأ <code>reports/day05_recovery.json</code>: يحتوي <code>run_ids</code> معرفي الإصدارين السليمين، ويجب نجاح عناصر <code>checks</code> الأربعة. يصبح أحدث إصدار سليم هو المختار.</p><p>هذا تعافٍ مضبوط داخل جلسة Spark واحدة، وليس محاكاة لتعطل الجهاز. أجب داخل <code>LAB07_NOTES.md</code> عن الأسئلة 1–4 في <a href="../../PRACTICE.md">صفحة التطبيق</a>، ثم نفذ <a href="../lab08/WALKTHROUGH.md">اللاب 08</a>. لا تحذف النسخة الفاشلة أو تحوّل المؤشر إليها يدويًا. <a href="../../INTEGRATION.md">التشخيصات الاختيارية</a> منفصلة عن هذا المسار المطلوب.</p></td></tr></table>
