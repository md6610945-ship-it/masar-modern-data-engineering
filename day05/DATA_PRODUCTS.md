<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Day 5 · Eight output tables</h1><p><a href="README.md">Day 5</a> · <a href="STUDENT.ipynb">Notebook</a> · <a href="AI_BI_GUIDE.md">AI/BI guide</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اليوم الخامس · جداول المخرجات الثمانية</h1><p><a href="README.md">اليوم الخامس</a> · <a href="STUDENT.ipynb">الدفتر</a> · <a href="AI_BI_GUIDE.md">دليل AI وBI</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Read the contract before the file</h2><p>The names below identify products within a release, not eight extra labs or automatically registered SQL tables. Their Delta paths are under <code>mini_lakehouse/serving_releases/&lt;release_id&gt;/</code>. Read the selected release manifest rather than guessing a folder.</p><p><a href="../src/masar/serving_reference.py">TABLE_COLUMNS and TABLE_KEYS</a> define the exact columns and keys; <a href="../src/masar/serving.py">build_frames</a> creates the actual Spark outputs. Counts below describe the fixed synthetic fixture, not production targets. Fares use SAR with two decimal places, distances kilometres and durations seconds.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>اقرأ تعريف البيانات قبل الملف</h2><p>تحدد الأسماء التالية مخرجات داخل إصدار، وليست ثمانية لابات إضافية أو جداول مسجلة تلقائيًا في SQL. تقع مسارات Delta تحت <code>mini_lakehouse/serving_releases/&lt;release_id&gt;/</code>. اقرأ سجل الإصدار المختار بدل تخمين مجلد.</p><p>تحدد <a href="../src/masar/serving_reference.py">TABLE_COLUMNS وTABLE_KEYS</a> الأعمدة والمفاتيح بدقة، وتنشئ <a href="../src/masar/serving.py">build_frames</a> المخرجات الفعلية في Spark. تخص الأعداد التالية البيانات الاصطناعية الثابتة، وليست أهداف إنتاج. الأجور بالريال بمنزلتين عشريتين، والمسافات بالكيلومتر، والمدد بالثانية.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>1. gold.zone_hourly_demand</h2><p><strong>Grain:</strong> one observed city proxy and UTC trip-start hour.<br><strong>Key:</strong> <code>zone_key + hour_utc</code>.<br><strong>Other fields:</strong> <code>hour_local</code>, <code>trip_count</code>, <code>total_fare_sar</code>, <code>total_duration_seconds</code>.<br><strong>Fixture:</strong> 75 groups; summed trip counts equal 75.</p><p><code>hour_local</code> carries the +03:00 display offset. Only observed hours appear. The name “demand” means observed trip starts here, not all requests or measured market demand. Having 75 rows is coincidental for this fixture; a group can contain several trips in other data.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>١. gold.zone_hourly_demand</h2><p><strong>معنى الصف:</strong> مدينة تمثيلية وساعة بداية رحلة UTC مرصودتان.<br><strong>المفتاح:</strong> <code>zone_key + hour_utc</code>.<br><strong>الحقول الأخرى:</strong> <code>hour_local</code> و<code>trip_count</code> و<code>total_fare_sar</code> و<code>total_duration_seconds</code>.<br><strong>في العينة:</strong> 75 مجموعة، ومجموع أعداد رحلاتها 75.</p><p>يحمل <code>hour_local</code> إزاحة العرض +03:00. تظهر الساعات المرصودة فقط. تعني كلمة «الطلب» هنا بدايات الرحلات المرصودة، لا كل طلبات النقل أو الطلب السوقي المقاس. تساوي عدد الصفوف مع 75 خاص بهذه العينة؛ فقد تضم المجموعة رحلات متعددة في بيانات أخرى.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>2. gold.driver_daily</h2><p><strong>Grain:</strong> one driver and local trip-start date.<br><strong>Key:</strong> <code>driver_key + trip_date_local</code>.<br><strong>Measures:</strong> <code>trip_count</code>, <code>total_fare_sar</code>, <code>total_duration_seconds</code>.<br><strong>Fixture:</strong> 18 groups.</p><p>Sum the measures to reconcile with the same 75 trips and SAR 1,880.60; do not compare the 18 group rows directly with the trip count.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٢. gold.driver_daily</h2><p><strong>معنى الصف:</strong> سائق وتاريخ بداية رحلة محلي.<br><strong>المفتاح:</strong> <code>driver_key + trip_date_local</code>.<br><strong>المقاييس:</strong> <code>trip_count</code> و<code>total_fare_sar</code> و<code>total_duration_seconds</code>.<br><strong>في العينة:</strong> 18 مجموعة.</p><p>اجمع المقاييس لتطابق الرحلات الـ75 وإجمالي 1,880.60 ريالًا نفسيهما؛ لا تقارن صفوف المجموعات الـ18 مباشرةً بعدد الرحلات.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>3. bi.dim_zone</h2><p><strong>Grain and key:</strong> one unique <code>zone_key</code>.<br><strong>Descriptions:</strong> <code>city</code> and <code>zone_resolution</code>.<br><strong>Fixture:</strong> three rows: <code>Z_RIYADH</code>, <code>Z_JEDDAH</code> and <code>Z_DAMMAM</code>.</p><p><code>zone_resolution=city_proxy</code> states the real level of this teaching definition. The data does not establish neighbourhoods or service-zone boundaries.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٣. bi.dim_zone</h2><p><strong>معنى الصف والمفتاح:</strong> قيمة فريدة لـ<code>zone_key</code>.<br><strong>الأوصاف:</strong> <code>city</code> و<code>zone_resolution</code>.<br><strong>في العينة:</strong> ثلاثة صفوف: <code>Z_RIYADH</code> و<code>Z_JEDDAH</code> و<code>Z_DAMMAM</code>.</p><p>تصرّح <code>zone_resolution=city_proxy</code> بمستوى هذا التعريف التدريبي. لا تثبت البيانات أحياءً أو حدود مناطق خدمة.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>4. bi.dim_driver</h2><p><strong>Grain and key:</strong> one unique <code>driver_key</code>.<br><strong>Other fields:</strong> <code>driver_id</code> and <code>vehicle_type</code>.<br><strong>Fixture:</strong> six synthetic drivers.</p><p>The simple natural key equals the synthetic source ID. This dimension does not implement changing-dimension history. Its descriptive fields are not automatically approved AI inputs.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٤. bi.dim_driver</h2><p><strong>معنى الصف والمفتاح:</strong> قيمة فريدة لـ<code>driver_key</code>.<br><strong>الحقول الأخرى:</strong> <code>driver_id</code> و<code>vehicle_type</code>.<br><strong>في العينة:</strong> ستة سائقين اصطناعيين.</p><p>يساوي المفتاح الطبيعي المبسط معرف المصدر الاصطناعي. لا ينفذ هذا البعد سجلًا تاريخيًا لتغير الصفات. وليست حقوله الوصفية مدخلات AI معتمدة تلقائيًا.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>5. bi.dim_date</h2><p><strong>Grain and key:</strong> one local date, keyed by integer <code>date_key</code> in YYYYMMDD form.<br><strong>Other fields:</strong> <code>date_local</code>, <code>year</code>, <code>month</code>, <code>day</code>.<br><strong>Fixture:</strong> June 1, 2 and 3, 2026.</p><p>The date follows the trip start in Asia/Riyadh. This compact dimension covers observed dates only; it is not a full BI calendar.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٥. bi.dim_date</h2><p><strong>معنى الصف والمفتاح:</strong> تاريخ محلي بمفتاح عددي <code>date_key</code> بصيغة YYYYMMDD.<br><strong>الحقول الأخرى:</strong> <code>date_local</code> و<code>year</code> و<code>month</code> و<code>day</code>.<br><strong>في العينة:</strong> 1 و2 و3 يونيو 2026.</p><p>يتبع التاريخ بداية الرحلة في Asia/Riyadh. يغطي هذا البعد الصغير التواريخ المرصودة فقط، وليس تقويم BI كاملًا.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>6. bi.fact_trips</h2><p><strong>Grain and key:</strong> one unique <code>trip_id</code>.<br><strong>Dimension keys:</strong> <code>driver_key</code>, <code>zone_key</code>, <code>date_key</code>.<br><strong>Values:</strong> <code>start_utc</code>, <code>fare_sar</code>, <code>distance_km</code>, <code>duration_seconds</code>, <code>gps_event_count</code>.<br><strong>Fixture:</strong> 75 rows.</p><p>Events are counted per trip before the join. The three late trips remain even without GPS events; their recorded event count is zero. Total recorded events are 217. No raw coordinates are exported in this table.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٦. bi.fact_trips</h2><p><strong>معنى الصف والمفتاح:</strong> رحلة فريدة بـ<code>trip_id</code>.<br><strong>مفاتيح الأبعاد:</strong> <code>driver_key</code> و<code>zone_key</code> و<code>date_key</code>.<br><strong>القيم:</strong> <code>start_utc</code> و<code>fare_sar</code> و<code>distance_km</code> و<code>duration_seconds</code> و<code>gps_event_count</code>.<br><strong>في العينة:</strong> 75 صفًا.</p><p>تُعد الأحداث لكل رحلة قبل الربط. تبقى الرحلات المتأخرة الثلاث ولو لم يكن لها حدث GPS؛ ويكون عدد أحداثها المسجلة صفرًا. مجموع الأحداث المسجلة 217. لا تُصدّر إحداثيات خام في هذا الجدول.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>7. ai.zone_hourly_features</h2><p><strong>Grain and key:</strong> one <code>zone_key + as_of_utc + prediction_hour_utc</code>.<br><strong>Historical values:</strong> <code>completed_trips_24h</code>, <code>avg_duration_seconds_24h</code>, <code>history_available</code>.<br><strong>Audit fields:</strong> <code>history_window_start_utc</code> and <code>max_source_available_at_utc</code>, alongside the cutoff and horizon.<br><strong>Fixture:</strong> three rows.</p><p>Include trips ending in <code>[cutoff − 24 hours, cutoff)</code> whose source revision was available by the cutoff. If eligible history is absent, the count is 0, the mean is null and <code>history_available=false</code>; this does not prove zero real-world activity. Read <a href="AI_BI_GUIDE.md">the fixed cutoff and input allowlist</a>.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٧. ai.zone_hourly_features</h2><p><strong>معنى الصف والمفتاح:</strong> مجموعة <code>zone_key + as_of_utc + prediction_hour_utc</code> واحدة.<br><strong>القيم التاريخية:</strong> <code>completed_trips_24h</code> و<code>avg_duration_seconds_24h</code> و<code>history_available</code>.<br><strong>حقول التدقيق:</strong> <code>history_window_start_utc</code> و<code>max_source_available_at_utc</code> إلى جانب وقت القطع والأفق.<br><strong>في العينة:</strong> ثلاثة صفوف.</p><p>تُدرج الرحلات المنتهية خلال <code>[وقت القطع − 24 ساعة، وقت القطع)</code> بشرط إتاحة مراجعة المصدر بحلول وقت القطع. عند غياب التاريخ المؤهل يكون العدد 0 والمتوسط فارغًا و<code>history_available=false</code>؛ ولا يثبت ذلك انعدام النشاط الواقعي. اقرأ <a href="AI_BI_GUIDE.md">وقت القطع الثابت وقائمة المدخلات المسموحة</a>.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>8. ai.zone_hourly_labels</h2><p><strong>Grain and key:</strong> one <code>zone_key + prediction_hour_utc</code>.<br><strong>Other fields:</strong> <code>target_trip_count</code>, <code>label_status</code>, <code>label_available_at_utc</code>.<br><strong>Fixture:</strong> three rows, all <code>UNOBSERVED</code>, with null targets and null label-availability times.</p><p>These rows document unavailable future ground truth. They are not three training examples with a zero target. The allowed inputs and future labels must remain separate.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٨. ai.zone_hourly_labels</h2><p><strong>معنى الصف والمفتاح:</strong> مجموعة <code>zone_key + prediction_hour_utc</code> واحدة.<br><strong>الحقول الأخرى:</strong> <code>target_trip_count</code> و<code>label_status</code> و<code>label_available_at_utc</code>.<br><strong>في العينة:</strong> ثلاثة صفوف بحالة <code>UNOBSERVED</code> وأهداف وتوقيتات إتاحة للنتائج فارغة.</p><p>توثق هذه الصفوف غياب الحقيقة المستقبلية المرصودة. ليست ثلاث عينات تدريب بأهداف صفرية. يجب الفصل بين المدخلات المسموحة والنتائج المستقبلية.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Retain versions and export meaning</h2><p>The release manifest records each table's path, Delta version, row count and canonical-content hash. Lab 08 rereads those versions and exports one CSV per product, replacing dots in the product name with underscores.</p><p>The small CSV writer represents <code>None</code> as an empty field and writes booleans as text. CSV does not carry the Delta schema: preserve this contract, units and null meaning. A file hash checks integrity, not the truth of an execution claim. See <a href="SOURCES.md">D5-6</a> and <a href="COMPLETION.md">completion</a>.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>احتفظ بالنسخ ومعنى التصدير</h2><p>يسجل ملف الإصدار مسار كل جدول ونسخة Delta وعدد الصفوف وبصمة المحتوى المعياري. يعيد اللاب 08 قراءة تلك النسخ ويصدر ملف CSV لكل مخرج، مع استبدال النقاط في اسم المخرج بشرطات سفلية.</p><p>يمثل كاتب CSV الصغير <code>None</code> بحقل فارغ ويكتب القيم المنطقية كنصوص. لا يحمل CSV مخطط Delta؛ فاحتفظ بهذا التعريف ووحداته ومعنى القيم الفارغة. تفحص بصمة الملف سلامته، ولا تثبت صحة ادعاء التنفيذ وحدها. راجع <a href="SOURCES.md">D5-6</a> و<a href="COMPLETION.md">الاكتمال</a>.</p></td></tr></table>
