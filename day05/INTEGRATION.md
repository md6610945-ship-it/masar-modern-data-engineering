<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Day 5 · Complete the existing pipeline</h1><p><a href="README.md">Day 5</a> · <a href="STUDENT.ipynb">Student notebook</a> · <a href="COMPLETION.md">Completion</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اليوم الخامس · أكمل خط البيانات القائم</h1><p><a href="README.md">اليوم الخامس</a> · <a href="STUDENT.ipynb">دفتر الطالب</a> · <a href="COMPLETION.md">الاكتمال</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Required route: continue, do not restart</h2><p>Complete Day 4, then run the four code cells in <a href="STUDENT.ipynb">the Day 5 notebook</a>. Lab 07 reads the approved trip and event snapshots, builds their outputs and tests controlled recovery. Lab 08 validates the selected release, runs the reporting query and exports it. The final cell saves your working state.</p><p>You do not need to rerun all earlier labs, restart Kafka, repeat dbt or submit a second project to complete this day. Your earlier executed notebooks and reports remain part of the same submission.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>المسار المطلوب: استكمل ولا تبدأ من جديد</h2><p>أكمل اليوم الرابع ثم شغّل الخلايا البرمجية الأربع في <a href="STUDENT.ipynb">دفتر اليوم الخامس</a>. يقرأ اللاب 07 لقطتي الرحلات والأحداث المعتمدتين ويبني مخرجاتهما ويختبر التعافي المضبوط. يتحقق اللاب 08 من الإصدار المختار وينفذ استعلام التقارير ويصدره. تحفظ الخلية الأخيرة حالة عملك.</p><p>لا يلزم إعادة جميع اللابات السابقة أو بدء Kafka أو تكرار dbt أو تسليم مشروع ثانٍ لإكمال هذا اليوم. تبقى دفاترك المنفذة وتقاريرك السابقة جزءًا من التسليم نفسه.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>What connects the days?</h2><p>Raw receipts → Bronze → typed staging → unique Silver trips → approved corrections → distinct streamed events and quality approval → eight serving tables → reporting and exports.</p><p>The input loader checks <code>reports/day04_quality_latest.json</code> and <code>reports/day04_stream_latest.json</code>, then reads the Delta paths named in them. It records both actual input versions and report hashes. Do not substitute the quarantine, a schema sandbox or a reference JSON file for these inputs.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>ما الذي يربط الأيام؟</h2><p>استقبال المصدر ثم Bronze، فالتهيئة وSilver الفريدة، ثم التصحيحات المعتمدة والأحداث المتدفقة المختلفة واعتماد الجودة، ثم جداول التقديم الثمانية والتقارير والتصدير.</p><p>يفحص تحميل المدخلات <code>reports/day04_quality_latest.json</code> و<code>reports/day04_stream_latest.json</code>، ثم يقرأ مساري Delta المحددين فيهما ويسجل نسختيهما الفعليتين وبصمتي التقريرين. لا تستبدل هذه المدخلات بالعزل أو نسخة تجربة المخطط أو ملف JSON مرجعي.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Understand the local release boundary</h2><p>Each new candidate has its own folder under <code>mini_lakehouse/serving_releases/</code>. Each Delta table commits separately. The helper writes <code>release.json</code> only after all eight tables pass, and updates <code>reports/day05_gold_latest.json</code> last.</p><p>The reader follows that pointer, verifies its manifest and reads recorded table versions. A failed candidate can leave files on disk, but this reader does not select them. This is an application-level selection rule, not one atomic transaction across eight tables or a deployment to GitHub.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>افهم حدود الإصدار المحلي</h2><p>لكل نسخة مرشحة جديدة مجلد مستقل تحت <code>mini_lakehouse/serving_releases/</code>. يعتمد كل جدول Delta معاملته منفصلًا. لا تكتب الدالة <code>release.json</code> إلا بعد نجاح الجداول الثمانية، وتحدث <code>reports/day05_gold_latest.json</code> أخيرًا.</p><p>يتبع القارئ المؤشر ويفحص سجل الإصدار ويقرأ نسخ الجداول المسجلة. قد تترك المحاولة الفاشلة ملفات على القرص، لكن هذا القارئ لا يختارها. هذه قاعدة اختيار في التطبيق، وليست معاملة ذرية واحدة للجداول الثمانية أو نشرًا إلى GitHub.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Optional diagnostic: fresh all-days integration</h2><p><a href="../scripts/run_day05.py">run_day05.py</a> also supports <code>--part integration</code>. This is an optional diagnostic route, not an additional daily task. It starts from fixed sources in a new marked workspace, requires the live local Kafka broker and the Spark/Delta/GX environment, and executes ten stages representing the same eight labs.</p><p>Use a separate complete repository copy for this diagnostic. Although previous output folders are retained, a successful new Bronze stage updates that copy's <code>outputs/day01_bronze_success.json</code> pointer. Running it in your active project would change which workspace later commands select.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>تشخيص اختياري: تكامل شامل من البداية</h2><p>يدعم <a href="../scripts/run_day05.py">run_day05.py</a> خيار <code>--part integration</code>. هو مسار تشخيص اختياري، وليس مهمة يومية إضافية. يبدأ من المصادر الثابتة داخل مساحة جديدة معلّمة، ويتطلب وسيط Kafka المحلي العامل وبيئة Spark وDelta وGX، وينفذ عشر مراحل تمثل اللابات الثمانية نفسها.</p><p>استخدم نسخة كاملة منفصلة من المستودع لهذا التشخيص. تبقى مجلدات المخرجات السابقة محفوظة، لكن نجاح مرحلة Bronze الجديدة يحدّث مؤشر <code>outputs/day01_bronze_success.json</code> في تلك النسخة. تشغيله داخل مشروعك النشط يغير مساحة العمل التي تختارها الأوامر اللاحقة.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Read diagnostic results accurately</h2><p>The optional runner records <code>reports/integration_stages.json</code> and stops descendants of a failed stage. Its status <code>PASSED_NATIVE_PATH_DBT_NOT_VALIDATED</code> means the native pipeline passed without executing dbt. Keep the separate Day 2 dbt result; do not relabel this status as full-course certification.</p><p>The daily notebook does not generate that optional integration journal. Its absence is not a missing Day 5 deliverable. Likewise, <code>check_submission.py</code> is an optional file inventory with its own metadata requirements, not the notebook's completion check or an automatic grade.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>اقرأ نتائج التشخيص بدقة</h2><p>يسجل المشغّل الاختياري <code>reports/integration_stages.json</code> ويوقف المراحل التابعة عند فشل مرحلة سابقة. تعني حالته <code>PASSED_NATIVE_PATH_DBT_NOT_VALIDATED</code> نجاح خط المعالجة الأصلي دون تنفيذ dbt. احتفظ بنتيجة dbt المنفصلة من اليوم الثاني، ولا تصف الحالة بوصفها اعتمادًا للدورة كاملة.</p><p>لا يولد دفتر اليوم سجل التكامل الاختياري المذكور؛ وغيابه ليس نقصًا في تسليم اليوم الخامس. كذلك يُعد <code>check_submission.py</code> أداة حصر اختيارية بمتطلبات وصفية خاصة، لا فحص اكتمال الدفتر ولا تقييمًا آليًا.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Keep test scopes separate</h2><p>Lab 07's good/fail/rebuild sequence occurs in one Spark session. The course's <a href="../docs/VERIFICATION.md">saved verification</a> separately records two independent five-day notebook runs. Neither is a distributed load test, crash-recovery test or claim of model accuracy.</p><p>Finish with <a href="COMPLETION.md">the daily checklist</a> and <a href="../project/SUBMISSION.md">the single final submission</a>.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>ميّز نطاقات الاختبار</h2><p>يحدث تسلسل البناء السليم والفشل وإعادة البناء في اللاب 07 داخل جلسة Spark واحدة. يوثق <a href="../docs/VERIFICATION.md">سجل تحقق الدورة</a> بصورة منفصلة تشغيلين مستقلين لدفاتر الأيام الخمسة. لا يمثل أي منهما اختبار ضغط موزع أو تعافيًا من تعطل الجهاز أو دقة نموذج.</p><p>اختم بـ<a href="COMPLETION.md">قائمة اليوم</a> و<a href="../project/SUBMISSION.md">التسليم النهائي الواحد</a>.</p></td></tr></table>
