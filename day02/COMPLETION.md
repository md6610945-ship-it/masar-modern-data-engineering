<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Day 2 · Check, save and continue</h1><p><a href="README.md">Day 2 guide</a> · <a href="STUDENT.ipynb">Your notebook</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اليوم الثاني · تحقق واحفظ ثم تابع</h1><p><a href="README.md">دليل اليوم</a> · <a href="STUDENT.ipynb">دفترك</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Check completion</h2><ol><li>The 03a checks pass; staging preserves 144 trip receipts, 6 drivers and 216 events.</li><li>The 03b checks pass; Silver has 75 unique trips and SAR 1,875.60 in total fares after late arrivals.</li><li>Replaying arrivals does not duplicate Silver business rows; the expected five scenarios reconcile.</li><li>dbt returns <code>PASSED_DBT_NATIVE</code>; its four phases retain 72, 72, 75 and 75 trips, and its catalog covers six models and three sources.</li><li>The final cell creates the handoff ZIP and its archive integrity check passes.</li><li>Your notebook contains your actual outputs and your lab notes explain the results.</li></ol></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>تحقق من الاكتمال</h2><ol><li>نجحت فحوص 03a، واحتفظت التهيئة بـ144 نسخة استقبال للرحلات و6 سائقين و216 حدثًا.</li><li>نجحت فحوص 03b، وأصبحت Silver تضم 75 رحلة فريدة بإجمالي أجور 1,875.60 ريالًا بعد الوصول المتأخر.</li><li>لم تضاعف إعادة الاستقبال صفوف الأعمال في Silver، وتطابقت السيناريوهات الخمسة.</li><li>أعاد dbt الحالة <code>PASSED_DBT_NATIVE</code>؛ وأعداد مراحله الأربع 72 و72 و75 و75 رحلة، وغطى كتالوجه ستة نماذج وثلاثة مصادر.</li><li>أنشأت الخلية الأخيرة ملف الانتقال ونجح فحص سلامة الأرشيف.</li><li>يحتوي دفترك على مخرجات تشغيلك، وتشرح ملاحظات اللاب النتائج.</li></ol></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Keep three learner deliverables</h2><p><strong>1.</strong> Your executed <code>day02/STUDENT.ipynb</code>.<br><strong>2.</strong> <code>LAB03_NOTES.md</code>: explain the row grain, one normalization example, a safe join, rerun/late-arrival results, and how <code>source()</code>, <code>ref()</code> and the lookback connect. Use <a href="PRACTICE.md">the eight questions</a> within these same notes.<br><strong>3.</strong> <code>outputs/day02_handoff.zip</code>, retained through the approved output-sharing route rather than force-added to Git.</p><p>These are evidence from the same Lab 03, not three assignments. Follow <a href="../docs/GIT_WORKFLOW.md">the Git guide</a> and <a href="../project/SUBMISSION.md">the cumulative submission rules</a>. The optional reference notebook is not required.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>احتفظ بثلاثة مخرجات</h2><p><strong>1.</strong> دفتر <code>day02/STUDENT.ipynb</code> بمخرجاتك الفعلية.<br><strong>2.</strong> ملف <code>LAB03_NOTES.md</code>: اشرح معنى الصف ومثال توحيد وربطًا آمنًا ونتائج الإعادة والوصول المتأخر وعلاقة <code>source()</code> و<code>ref()</code> ونافذة المراجعة. استخدم <a href="PRACTICE.md">الأسئلة الثمانية</a> داخل الملاحظات نفسها.<br><strong>3.</strong> ملف <code>outputs/day02_handoff.zip</code>، محفوظًا عبر قناة مشاركة المخرجات المعتمدة، لا مضافًا قسريًا إلى Git.</p><p>هذه أدلة اللاب 03 نفسه، وليست ثلاثة تكليفات. اتبع <a href="../docs/GIT_WORKFLOW.md">دليل Git</a> و<a href="../project/SUBMISSION.md">قواعد التسليم التراكمي</a>. المرجع الحسابي الاختياري غير مطلوب للتسليم.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Where to find the evidence</h2><p>Under the <code>WORK</code> folder printed by the notebook, inspect <code>reports/day02_staging_latest.json</code>, <code>reports/day02_silver.json</code> and <code>reports/day02_expected_vs_actual.json</code>. The continuing table is <code>mini_lakehouse/silver/trips/</code>.</p><p>The dbt cell prints the path to <code>reports/dbt_attempt.json</code> in its separate workspace. Each dbt command retains its own logs and target folder; the documentation command retains <code>index.html</code>, <code>manifest.json</code> and <code>catalog.json</code>. The final notebook cell includes this workspace in the handoff automatically.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>أين تجد الأدلة؟</h2><p>داخل مجلد <code>WORK</code> الذي يطبعه الدفتر، افحص <code>reports/day02_staging_latest.json</code> و<code>reports/day02_silver.json</code> و<code>reports/day02_expected_vs_actual.json</code>. الجدول الذي ستكمل عليه هو <code>mini_lakehouse/silver/trips/</code>.</p><p>تطبع خلية dbt مسار <code>reports/dbt_attempt.json</code> في مساحته المنفصلة. يحتفظ كل أمر dbt بسجلاته ومجلد نتائجه، ويحفظ أمر التوثيق <code>index.html</code> و<code>manifest.json</code> و<code>catalog.json</code>. تضمّن الخلية الأخيرة هذه المساحة في ملف الانتقال تلقائيًا.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>Continue without rebuilding the dataset</h2><p>Keep using the same repository and successful workspace. If you move to a new session, restore your own Day 2 ZIP at the repository root, retaining the <code>outputs/</code> paths and pointer. Do not overwrite a different live workspace; use a separate repository copy when comparing attempts.</p><p>Day 3 receives the existing 75-trip Delta Silver table. Do not load <code>correction.csv</code> during Day 2; its revision-aware correction belongs to the next day.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>تابع دون إعادة بناء البيانات</h2><p>استمر بالمستودع ومساحة العمل الناجحة نفسيهما. عند الانتقال إلى جلسة جديدة، استعد ملف اليوم الثاني الخاص بك في جذر المستودع، مع الإبقاء على مسارات <code>outputs/</code> والمؤشر. لا تكتب فوق مساحة عمل أخرى نشطة؛ استخدم نسخة مستودع منفصلة لمقارنة المحاولات.</p><p>يستقبل اليوم الثالث جدول Delta Silver القائم برحلاته الـ75. لا تحمّل <code>correction.csv</code> في اليوم الثاني؛ فتصحيح السجل بحسب مراجعة المصدر يخص اليوم التالي.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>If a check fails</h2><p>Keep the error, the last successful cell and the printed workspace path. A missing pointer requires restoring the whole handoff, not creating an empty pointer. A partially written Day 2 is preserved; for a clean retry, run Day 1 to create a new workspace, then rerun Day 2 in order. Do not delete the interrupted workspace.</p><p>A dbt freshness warning or failure concerns the original arrival timestamps. Do not change those timestamps to obtain a pass. Use <a href="DBT_GUIDE.md">the freshness explanation</a> and <a href="../docs/TROUBLESHOOTING.md">troubleshooting</a>.</p><p><a href="verification.json">Saved Day 2 execution evidence</a> · <a href="../day03/README.md">Next day</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>عند فشل أحد الفحوص</h2><p>احتفظ بالخطأ وآخر خلية ناجحة ومسار مساحة العمل المطبوع. فقدان المؤشر يتطلب استعادة ملف الانتقال كاملًا، لا إنشاء مؤشر فارغ. تُحفظ محاولة اليوم الثاني الجزئية؛ ولإعادة المحاولة من بداية سليمة، شغّل اليوم الأول لإنشاء مساحة جديدة ثم نفّذ اليوم الثاني بالترتيب، دون حذف المحاولة المتوقفة.</p><p>يتعلق تحذير الحداثة أو فشلها في dbt بتوقيتات الوصول الأصلية. لا تغيّر التوقيتات للحصول على نجاح صوري. راجع <a href="DBT_GUIDE.md">شرح الحداثة</a> و<a href="../docs/TROUBLESHOOTING.md">معالجة الأخطاء</a>.</p><p><a href="verification.json">دليل تنفيذ اليوم الثاني المحفوظ</a> · <a href="../day03/README.md">اليوم التالي</a></p></td></tr></table>

