<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h1>Lab 03 · Build Silver step by step</h1><p><a href="../../README.md">Day 2</a> · <a href="../../STUDENT.ipynb"><strong>Student notebook</strong></a> · <a href="../../DATA_CONTRACT.md">Field rules</a></p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h1>اللاب 03 · ابنِ Silver خطوة بخطوة</h1><p><a href="../../README.md">اليوم الثاني</a> · <a href="../../STUDENT.ipynb"><strong>دفتر الطالب</strong></a> · <a href="../../DATA_CONTRACT.md">قواعد الحقول</a></p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>1. Open the correct workspace</h2><p>Run the first code cell in <code>day02/STUDENT.ipynb</code>. It checks the fixed dataset, the environment and the successful Day 1 pointer. <code>Continue workspace:</code> should identify your existing output folder.</p><p>Use the complete repository. On a new session, restore your own Day 1 handoff first. Do not substitute a CSV for a missing Delta table. This day reads the original snapshots: trips version 1, drivers version 0 and GPS version 0.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>١. افتح مساحة العمل الصحيحة</h2><p>شغّل أول خلية برمجية في <code>day02/STUDENT.ipynb</code>. تفحص البيانات الثابتة والبيئة ومؤشر اليوم الأول الناجح. يجب أن تشير رسالة <code>Continue workspace:</code> إلى مجلد مخرجاتك القائم.</p><p>احتفظ بالمستودع كاملًا واستعد ملف انتقال اليوم الأول عند استخدام جلسة جديدة. لا تستبدل جدول Delta مفقودًا بملف CSV. يقرأ هذا اليوم لقطات الأساس: نسخة الرحلات 1 والسائقين 0 والمواقع 0.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>2. Part 03a: type and standardize the data</h2><p>Run the <strong>03a</strong> cell. <code>run_staging_lab</code> prepares three Delta snapshots: <code>stg_trips</code>, <code>stg_drivers</code> and <code>stg_gps</code>. Inspect the printed sample and the checks before continuing.</p><p>Expected staging counts: <strong>144 trip receipts, 6 drivers and 216 GPS events</strong>. A repeated trip in the sample is correct at this point: typing is not deduplication. For example, the two receipts for <code>SYN_T0001</code> both retain the fare <code>18.00</code>.</p><p>Known city labels are standardized, decimal values are checked and timestamp offsets are parsed once. The trip date remains its local start date in Asia/Riyadh, even when the corresponding UTC date differs.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٢. الجزء 03a: هيئ الأنواع والقيم</h2><p>شغّل خلية <strong>03a</strong>. تُعد الدالة <code>run_staging_lab</code> ثلاث لقطات Delta: <code>stg_trips</code> و<code>stg_drivers</code> و<code>stg_gps</code>. افحص العينة المطبوعة والفحوص قبل المتابعة.</p><p>الأعداد المتوقعة للتهيئة: <strong>144 نسخة استقبال للرحلات و6 سائقين و216 حدث مواقع</strong>. تكرار الرحلة في العينة صحيح هنا؛ فتحويل النوع ليس إزالة تكرار. مثلًا، تحتفظ نسختا <code>SYN_T0001</code> بالأجرة <code>18.00</code>.</p><p>تُوحّد مسميات المدن المعروفة وتُفحص القيم العشرية وتُقرأ إزاحة التوقيت مرة واحدة. يبقى تاريخ الرحلة هو تاريخ بدايتها المحلي في Asia/Riyadh، ولو اختلف تاريخ UTC المقابل.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>3. Check relationships before choosing a row</h2><p>The shared implementation rejects duplicate driver keys and missing driver relationships before enrichment. A valid trip-to-driver join preserves the receipt count. Conflicting business values at the same source revision also stop processing; they are not ordinary duplicates.</p><p>Only identical receipts are reduced using a deterministic ordering. You obtain one candidate per <code>trip_id</code>. Do not join the 216 raw GPS events directly into a fare total: each base trip has three events, so that join would count its fare three times.</p><p>Read <code>validate_staging</code> and <code>silver_candidates</code> in <a href="../../../src/masar/silver.py">the shared implementation</a>. These functions are already called by the notebook; you do not need to copy them into new cells.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٣. افحص العلاقات قبل اختيار السجل</h2><p>يرفض التنفيذ المشترك مفاتيح السائقين المكررة والعلاقات المفقودة قبل إضافة صفات السائق. يحافظ الربط الصحيح على عدد نسخ الاستقبال. كذلك يوقف اختلاف قيم الأعمال مع مراجعة المصدر نفسها المعالجة؛ فهو ليس تكرارًا عاديًا.</p><p>تُختصر النسخ المتطابقة فقط بترتيب ثابت إلى سجل مرشح واحد لكل <code>trip_id</code>. لا تربط أحداث المواقع الخام الـ216 مباشرةً بمجموع الأجور؛ لكل رحلة أساسية ثلاثة أحداث، وسيُحتسب أجرها ثلاث مرات.</p><p>اقرأ الدالتين <code>validate_staging</code> و<code>silver_candidates</code> في <a href="../../../src/masar/silver.py">التنفيذ المشترك</a>. يستدعيهما الدفتر بالفعل، ولا تحتاج إلى نسخهما في خلايا جديدة.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>4. Part 03b: build Silver and test replays</h2><p>Run the <strong>03b</strong> cell. It validates the staging report, creates or verifies the Silver Delta table, and checks five scenarios. Each row below gives <strong>Bronze receipts / Silver trips / total fares in SAR</strong>.</p><p>Base: <strong>144 / 72 / 1,794.60</strong><br>Transform rerun: <strong>144 / 72 / 1,794.60</strong><br>Late batch <code>late_003</code>: <strong>147 / 75 / 1,875.60</strong><br>Retry the same batch: <strong>147 / 75 / 1,875.60</strong><br>New delivery <code>late_replay_004</code>: <strong>150 / 75 / 1,875.60</strong></p><p>Day 2 MERGE inserts missing keys and leaves identical existing business rows unchanged. <a href="../../SOURCES.md">Delta and dbt references</a> explain why the source key must be unambiguous before MERGE.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٤. الجزء 03b: ابنِ Silver واختبر الإعادة</h2><p>شغّل خلية <strong>03b</strong>. تتحقق من تقرير التهيئة وتنشئ جدول Silver بصيغة Delta أو تفحصه، ثم تختبر خمسة سيناريوهات. يعرض كل سطر <strong>نسخ Bronze / رحلات Silver / إجمالي الأجور بالريال</strong>.</p><p>الأساس: <strong>144 / 72 / 1,794.60</strong><br>إعادة التحويل: <strong>144 / 72 / 1,794.60</strong><br>الدفعة المتأخرة <code>late_003</code>: <strong>147 / 75 / 1,875.60</strong><br>إعادة محاولة الدفعة نفسها: <strong>147 / 75 / 1,875.60</strong><br>إرسال جديد <code>late_replay_004</code>: <strong>150 / 75 / 1,875.60</strong></p><p>يضيف MERGE في اليوم الثاني المفاتيح المفقودة ويُبقي محتوى الأعمال المتطابق دون تغيير. توضح <a href="../../SOURCES.md">مراجع Delta وdbt</a> ضرورة إزالة غموض مفتاح المصدر قبل الدمج.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>5. Explain the late-arrival result</h2><p>The three late trips add <strong>SAR 81.00</strong>. They belong to June 1, 2026 even though the scenario delivers them on June 4. Counts by local trip date become <strong>27, 24 and 24</strong> for June 1, 2 and 3.</p><p>A filter such as <code>trip_date_local = current_date()</code> asks for trips occurring today; it does not ask which records arrived since the previous run. With this historical fixture it may return no rows at all. The dbt model uses the ingestion clock for its lookback instead.</p><p>Open <code>reports/day02_silver.json</code> under the printed workspace to inspect all five scenario records. Successful checks compare full business content, not just counts. Transaction versions and runtime durations may differ between correct runs.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٥. فسّر نتيجة الوصول المتأخر</h2><p>تضيف الرحلات المتأخرة الثلاث <strong>81.00 ريالًا</strong>. تنتمي إلى 1 يونيو 2026، رغم وصولها في السيناريو يوم 4 يونيو. تصبح أعداد الرحلات بحسب تاريخ البداية المحلي <strong>27 ثم 24 ثم 24</strong> لأيام 1 و2 و3 يونيو.</p><p>يبحث شرط مثل <code>trip_date_local = current_date()</code> عن رحلات وقعت اليوم، وليس عن سجلات وصلت منذ التشغيل السابق. ومع هذه البيانات التاريخية قد يعيد صفر صفوف. يستخدم نموذج dbt وقت الاستقبال لنافذة المراجعة بدلًا منه.</p><p>افتح <code>reports/day02_silver.json</code> تحت مساحة العمل المطبوعة لفحص السيناريوهات الخمسة. تقارن الفحوص محتوى الأعمال كاملًا لا العدد وحده. قد تختلف أرقام نسخ المعاملات وأزمنة التنفيذ بين تشغيلين صحيحين.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>6. Run dbt in the same daily notebook</h2><p>Read <a href="../../DBT_GUIDE.md">the dbt guide</a>, then run the <strong>Lab 03 · Run the dbt models</strong> cell. The runner uses isolated copies of the original Bronze snapshots. It does not replace the Silver table that will continue to Day 3.</p><p>Inspect the returned status <code>PASSED_DBT_NATIVE</code> and the four printed phases: <code>base</code>, <code>rerun</code>, <code>late</code> and <code>late_replay</code>. The row counts are <strong>72, 72, 75, 75</strong>; the corresponding totals are <strong>1,794.60, 1,794.60, 1,875.60, 1,875.60 SAR</strong>.</p><p>The report also verifies the generated catalog against <strong>six real models and three real source tables</strong>. The separate same-batch retry is exercised by 03b; it is not a missing fifth dbt phase.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٦. شغّل dbt داخل دفتر اليوم نفسه</h2><p>اقرأ <a href="../../DBT_GUIDE.md">دليل dbt</a> ثم شغّل خلية <strong>Lab 03 · Run the dbt models</strong>. يستخدم المشغّل نسخًا معزولة من لقطات Bronze الأصلية، ولا يستبدل جدول Silver الذي ستتابع به في اليوم الثالث.</p><p>افحص الحالة <code>PASSED_DBT_NATIVE</code> والمراحل الأربع المطبوعة: <code>base</code> و<code>rerun</code> و<code>late</code> و<code>late_replay</code>. أعدادها <strong>72، 72، 75، 75</strong>، وإجمالي أجورها بالترتيب <strong>1,794.60 و1,794.60 و1,875.60 و1,875.60 ريالًا</strong>.</p><p>يتحقق التقرير أيضًا من تغطية الكتالوج المولد <strong>ستة نماذج فعلية وثلاثة جداول مصدر فعلية</strong>. يختبر الجزء 03b إعادة محاولة الدفعة نفسها بصورة منفصلة؛ ليست مرحلة dbt خامسة مفقودة.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>7. Read the models and tests</h2><p>Open <a href="../../dbt/models/staging/stg_trips.sql">stg_trips.sql</a> to trace field conversion and <a href="../../dbt/models/silver/silver_trips.sql">silver_trips.sql</a> to inspect the three-day ingestion lookback. Use <a href="../../dbt/models/schema.yml">schema.yml</a> for key/null tests and <a href="../../dbt/tests/">tests/</a> for the seven SQL tests. Record what <code>source()</code> and <code>ref()</code> connect.</p><p>In this pinned local-session setup, use the notebook runner rather than starting <code>dbt build</code> in an unrelated process. See the guide for the freshness clock, retained documentation and the optional all-history reprocessing switch.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٧. اقرأ النماذج والاختبارات</h2><p>افتح <a href="../../dbt/models/staging/stg_trips.sql">stg_trips.sql</a> لتتبع تحويل الحقول، و<a href="../../dbt/models/silver/silver_trips.sql">silver_trips.sql</a> لفحص نافذة الثلاثة أيام بحسب وقت الاستقبال. راجع <a href="../../dbt/models/schema.yml">schema.yml</a> لاختبارات المفاتيح والقيم المفقودة، و<a href="../../dbt/tests/">tests/</a> لاختبارات SQL السبعة. وثّق ما يربطه كل من <code>source()</code> و<code>ref()</code>.</p><p>في إعداد الجلسة المحلية المرفق، استخدم مشغّل الدفتر بدل تنفيذ <code>dbt build</code> بعملية منفصلة. يشرح الدليل ساعة الحداثة والتوثيق المحفوظ وخيار إعادة معالجة السجل كاملًا عند الحاجة.</p></td></tr></table>

<table dir="ltr" width="100%"><tr><td width="50%" dir="ltr" lang="en" align="left" valign="top"><h2>8. Save the stage and handle errors safely</h2><p>Run the final notebook cell to create <code>outputs/day02_handoff.zip</code>. It contains the successful-workspace pointer, your cumulative Delta files and reports, and the isolated dbt workspace with its generated documentation. Save the executed notebook and <code>LAB03_NOTES.md</code> separately.</p><p>If a check fails, stop at that cell and retain its message. Do not delete previous outputs, bypass assertions or rerun Day 2 over a table already corrected on Day 3. Use <a href="../../COMPLETION.md">the completion and recovery guidance</a>.</p><p><a href="../../REFERENCE.ipynb">The calculation reference</a> is optional; it helps explain the expected values but is not another required execution route.</p></td><td width="50%" dir="rtl" lang="ar" align="right" valign="top"><h2>٨. احفظ المرحلة وتعامل مع الخطأ بأمان</h2><p>شغّل الخلية الأخيرة لإنشاء <code>outputs/day02_handoff.zip</code>. يحفظ مؤشر مساحة العمل الناجحة وملفات Delta وتقاريرك التراكمية ومساحة dbt المعزولة بتوثيقها المولد. احفظ الدفتر المنفذ و<code>LAB03_NOTES.md</code> بصورة منفصلة.</p><p>عند فشل فحص، توقف عند الخلية واحتفظ برسالتها. لا تحذف المخرجات السابقة أو تتجاوز الفحوص أو تعاود تشغيل اليوم الثاني على جدول صُحح في اليوم الثالث. اتبع <a href="../../COMPLETION.md">إرشادات الاكتمال والاستعادة</a>.</p><p><a href="../../REFERENCE.ipynb">المرجع الحسابي</a> اختياري؛ يساعدك على فهم القيم المتوقعة وليس مسار تشغيل إلزاميًا آخر.</p></td></tr></table>

